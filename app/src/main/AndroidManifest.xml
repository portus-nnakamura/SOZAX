<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sozax">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MILIS">

        <!-- トップ画面 -->
        <activity android:name="com.example.sozax.ui.TopActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ログイン画面 -->
        <activity android:name="com.example.sozax.ui.LoginActivity"/>
        <!-- メニュー画面 -->
        <activity android:name="com.example.sozax.ui.MenuActivity"/>
        <!-- 出庫処理(1/2) -->
        <activity android:name="com.example.sozax.ui.GoodsIssuePage1Activity" />
        <!-- 出庫処理(2/2) -->
        <activity android:name="com.example.sozax.ui.GoodsIssuePage2Activity" />
        <!-- 出庫伝票一覧 -->
        <activity android:name="com.example.sozax.ui.GoodsIssueListActivity" />
        <!-- 在庫照会(1/2) -->
        <activity android:name="com.example.sozax.ui.InventoryInquiryPage1Activity" />
        <!-- 在庫照会(2/2) -->
        <activity android:name="com.example.sozax.ui.InventoryInquiryPage2Activity" />

    </application>

</manifest>